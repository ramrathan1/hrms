node_modules
dist
.env
src/generated
*.log
# Anchored: the uploaded-blob directory (STORAGE_ROOT) at the server root only.
# Unanchored, this also swallowed src/infra/storage/ — which is why that module
# went missing from the repository.
/storage
